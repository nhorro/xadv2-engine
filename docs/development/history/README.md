# Historical design documents

The markdown under [`../design/`](../design/00-index.md) is **frozen history**.
It is the design that produced engine v2: requirements tags, MVP notes, issue
numbers, and a spec that mixed target and as-built.

Do not add features by extending those pages. Do not treat them as the
source of truth for new work.

The as-built tour and the debt plan live in [`../tour/`](../tour/index.md).

Author-facing YAML and Lua references live under `docs/authoring/`.
When those diverge from `design/05` and `design/06`, fix the authoring
pages and the code. Leave the historical design files alone unless you
are writing a dated note here.
