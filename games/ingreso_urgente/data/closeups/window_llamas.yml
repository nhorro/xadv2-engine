# Close-up / examine view of the bones Julia has to classify (issue #76).
# Opened from lab.lua via open_closeup("lab_skull_closeup"); backing out
# (Esc / right-click) pops back to the lab with its state intact.
#
# Static data only: the background, the hotspot polygons (in 1280x720 close-up
# space) and their hover names. Behavior lives in lab_skull.lua (the `logic:`
# sidecar wired in game.yaml) — clicking a hotspot runs its Lua handler.
version: 1
id: window_llamas

background: closeups/window_llamas.png
background_color: { r: 20, g: 16, b: 12 }

hotspots:
  # craneo:
  #   name: "el cráneo grande"
  #   area:
  #     - { x: 110, y: 120 }
  #     - { x: 560, y: 120 }
  #     - { x: 560, y: 470 }
  #     - { x: 110, y: 470 }
  # huesos:
  #   name: "los huesos sueltos"
  #   area:
  #     - { x: 690, y: 360 }
  #     - { x: 1200, y: 360 }
  #     - { x: 1200, y: 580 }
  #     - { x: 690, y: 580 }
