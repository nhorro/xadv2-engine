-- Empty behavior for the {{short_name}} sandbox. Add `on_load` / spawn(...)
-- coroutines here when you start exploring mechanics.
--
-- Hotspot handlers go under a `hotspots` sub-table, keyed by the hotspot id from
-- the room YAML. One placed at the top level is silently never called:
--
--   return {
--     on_load = function() end,
--     hotspots = {
--       sign = { look_at = function() return "Un cartel oxidado." end },
--     },
--   }
return {}
