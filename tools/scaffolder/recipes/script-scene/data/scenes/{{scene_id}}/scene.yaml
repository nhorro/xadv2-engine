# ScriptScene {{scene_id}}, generated by `tools.scaffolder add script-scene`.
# Coordinates use the game's virtual resolution.

version: 1
id: {{scene_id}}

background:
  color: { r: 24, g: 28, b: 40 }

entities: {}

# Add entities by id. Each needs exactly one visual component:
#
# entities:
#   marker:
#     sprite:
#       image: /ui/marker.png
#       origin: { x: 12, y: 12 }
#     transform:
#       position: { x: 640, y: 360 }
#     z: 10
#
#   actor:
#     animation:
#       source: /characters/actor/actor.anim.yml
#       sequence: idle
#     transform:
#       position: { x: 640, y: 560 }
